import AuthSection from "@/components/auth/login-signup";
import React from "react";

const page = () => {
  return (
    <div>
      <AuthSection page="login" />
    </div>
  );
};

export default page;
